[package]
name = "domination_uart"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.100"
postcard = { version = "1.1.1", features = ["alloc"] }
serde = { version = "1.0.228", default-features = false, features = ["derive", "alloc"] }

[dev-dependencies]
